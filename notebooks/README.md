# Notebooks

Use `Early_CVD_Prediction_ML_Lab.ipynb` as the final ML Lab notebook. It follows the report sections, loads the generated project artifacts, shows the data audit, compares models, presents ablation/sensitivity studies, and runs an explainable individual prediction.

The exploratory notebook has been audited in `docs/current_work_audit.md`. The final reported results come from the production pipeline in `src/`.
